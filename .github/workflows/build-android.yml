name: Build Android UE Dumper

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Download Android NDK r25c
        run: |
          wget https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip -q android-ndk-r25c-linux.zip
          # 设置 NDK_HOME 环境变量（Makefile 需要）
          echo "NDK_HOME=$PWD/android-ndk-r25c" >> $GITHUB_ENV
          # 可选：也设置 ANDROID_NDK_ROOT（可能有其他工具需要）
          echo "ANDROID_NDK_ROOT=$PWD/android-ndk-r25c" >> $GITHUB_ENV
          # 将 NDK 目录加入 PATH（非必须，但无害）
          echo "$PWD/android-ndk-r25c" >> $GITHUB_PATH

      - name: Compile project
        run: |
          cd ./AndUEDumper
          make clean && make
          ls -la out/

      - name: Upload compiled artifact
        uses: actions/upload-artifact@v4
        with:
          name: compiled-binary
          path: "**/out/*"
